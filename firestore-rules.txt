// ═══════════════════════════════════════════════════════════
// REGRAS DE SEGURANÇA DO FIRESTORE
// Cole essas regras no Firebase Console:
// Firestore → Regras → cole tudo → Publicar
//
// IMPORTANTE: troque SEU_EMAIL_AQUI pelo seu e-mail real
// ═══════════════════════════════════════════════════════════

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Função auxiliar: verifica se é admin
    function isAdmin() {
      return request.auth != null && request.auth.token.email == 'andrei-94@hotmail.com';
    }
    
    // Users: usuário lê/edita só o próprio doc. Admin lê todos.
    match /users/{userId} {
      allow create: if request.auth != null && request.auth.uid == userId;
      allow read, update: if request.auth != null && (request.auth.uid == userId || isAdmin());
      allow delete: if isAdmin();
    }
    
    // Questionnaires: usuário lê/escreve só o próprio. Admin lê todos.
    match /questionnaires/{userId} {
      allow create, update: if request.auth != null && request.auth.uid == userId;
      allow read: if request.auth != null && (request.auth.uid == userId || isAdmin());
    }
    
    // Sessions: usuário cria com seu próprio userId. Admin lê todas.
    match /sessions/{sessionId} {
      allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
      allow read: if request.auth != null && (resource.data.userId == request.auth.uid || isAdmin());
      allow list: if request.auth != null && isAdmin();
    }
  }
}
